extends layout

block content
  h1= title

  p Do you really want to delete this Account?
    |  This action is irrevorable

    form(method='POST' action='')
      div.form-group
        input#authorid.form-control(type='hidden',name='authorid', required='true' )

      button.btn.btn-primary(type='submit') Delete